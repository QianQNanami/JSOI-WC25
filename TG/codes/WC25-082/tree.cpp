#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int n, v[N], p[N];
signed main() {
	ios::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);
	freopen("tree.in", "r", stdin);
	freopen("tree.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++)
		cin >> v[i] >> p[i];
	cout << 1;
	return 0;
} 
